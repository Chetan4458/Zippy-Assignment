spring:
  datasource:
    url: jdbc:h2:mem:zippytest;MODE=PostgreSQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  flyway:
    locations: classpath:db/migration

zippy:
  automation-enabled: false
